# TASK-004 P0.0 / P0.1 Extraction Verification

## Verification Scope

This record verifies the owner-directed extraction of BAI Development OS from the `javascript-roulette` consumer repository and the initial Adaptive Development Governance implementation.

## Repository Lineage

- OS history base: former `baisound/ai-team` repository.
- OS input HEAD: `14d304c4ef7cb295d967b05c7c08616dbf98f090`.
- Imported implementation/evidence source: `baisound/javascript-roulette`.
- JavaScript Roulette input HEAD: `10cd2c8f53b79bc32a05154e0d4aa70389a44699`.
- Phase 1.5 Context Guard milestone evidence includes source commit `1caa275`.
- Target OS repository/root: `baisound/bai-development-os` / `/home/baisound/bai-development-os`.
- JavaScript Roulette remains `baisound/javascript-roulette` and becomes a consumer/reference project.

## Boundary Result

PASS.

- OS-owned Context Guard and Lifecycle runtime removed from JavaScript Roulette and placed in BAI Development OS.
- TASK-004 and OS-owned lifecycle/governance evidence live in BAI Development OS.
- JavaScript Roulette retains project-local TASK-001 through TASK-003.
- JavaScript Roulette integrates through `.bai-os/project.json`.
- Generic OS runtime no longer hardcodes `/home/baisound/projects/ai-team`, `/home/baisound/projects/javascript-roulette`, or a fixed `javascript-roulette` project ID.
- Product-boundary regression checker returns `BOUNDARY_CHECK_PASS`.

## Adaptive Development Governance Result

PASS for the initial foundation.

The runtime selector provides five development profiles:

- `DEV_0_QUICK`
- `DEV_1_LIGHT`
- `DEV_2_STANDARD`
- `DEV_3_HIGH_ASSURANCE`
- `DEV_4_FOUNDATION_CRITICAL`

Process depth is selected from system scale, feature scale, criticality, failure impact, reversibility, novelty, change kind, and high-risk flags. Mandatory safety floors prevent CORE/FOUNDATION/CRITICAL work from being under-classified. The selector changes workflow/test/review depth and explicitly leaves permanent model-routing policy unchanged.

## Test Evidence

Executed in the extracted working tree on 2026-08-08:

| Verification | Result |
|---|---|
| BAI Development OS `npm test` | PASS — 134/134 tests |
| BAI Development OS `npm run check:boundaries` | PASS — `BOUNDARY_CHECK_PASS` |
| JavaScript Roulette `node --test tests/roulette-core.test.mjs` | PASS — 10/10 tests |
| JavaScript Roulette direct Vite build (`node node_modules/vite/bin/vite.js build`) | PASS — Vite 7.3.6, 5 modules transformed |

The input ZIP flattened the `node_modules/.bin/vite` symbolic link into a normal file, so `npm run build` through that copied dependency tree is not valid evidence. `node_modules` and generated `dist` are intentionally excluded from the migration package. After placement, run `npm ci` to recreate dependencies and then run the normal `npm run build` command.

## Registry Result

The post-extraction OS document registry was reconciled after moving project-local TASK-001 through TASK-003 out of the OS tree. Final refresh result: `94 registered documents / 0 missing / 0 hash-or-size mismatches`.

## Authorization Boundary

P0.0 and P0.1 were performed under the current owner direction to correct the product boundary and make governance depth adaptive. This migration does not silently authorize unrelated TASK-004 Phase 1.6 implementation.
