import { createExtensionManifest } from './manifest.mjs';
export function createReferenceDomainManifest(domain,{extension_id=`bai.reference.${String(domain).toLowerCase()}`,trust_level='OFFICIAL',execution_mode='DECLARATIVE'}={}){return createExtensionManifest({extension_id,display_name:`BAI Reference ${domain}`,extension_version:'1.0.0',domains:[domain],trust_level,execution_mode,capabilities:[{capability_id:'domain.artifact_validator',operations:['validate']},{capability_id:'domain.artifact_previewer',operations:['preview']},{capability_id:'domain.quality_gate',operations:['evaluate']}],hook_types:[],metadata:{reference:true}});}
export function createReferenceDomainCatalog(){return ['VIDEO','AUDIO','BGM_SE','STREAMING','UNITY','WEB','DESKTOP','AUTOMATION'].map(d=>createReferenceDomainManifest(d));}
