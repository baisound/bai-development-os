import { ReleaseError } from './errors.mjs';
import { deepFreeze } from './util.mjs';
export function selectAcquisitionSource({air_gapped=false,cache=null,mirrors=[],registry=null}={}){const candidates=[];if(cache?.available)candidates.push({type:'CACHE',source:cache.source,verified:Boolean(cache.verified)});for(const m of mirrors)if(m.available)candidates.push({type:'MIRROR',source:m.source,verified:Boolean(m.verified)});if(!air_gapped&&registry?.available)candidates.push({type:'REGISTRY',source:registry.source,verified:Boolean(registry.verified)});const chosen=candidates.find(x=>x.verified);if(!chosen)throw new ReleaseError(air_gapped?'RELEASE_OFFLINE_SOURCE_UNAVAILABLE':'RELEASE_ACQUISITION_SOURCE_UNAVAILABLE');return deepFreeze({decision:'USE',...chosen,air_gapped});}
