import test from 'node:test'; import assert from 'node:assert/strict'; import { createSandboxPolicy, authorizeSandboxAction } from '../../src/security/sandbox.mjs';
test('sandbox allows only declared capability and path',()=>{const p=createSandboxPolicy({capabilities:['FILESYSTEM_READ'],read_roots:['fixtures'],max_runtime_ms:100});assert.equal(authorizeSandboxAction(p,{capability:'FILESYSTEM_READ',path:'fixtures/a.txt'}),true);assert.throws(()=>authorizeSandboxAction(p,{capability:'FILESYSTEM_READ',path:'outside/a.txt'}),e=>e.code==='SECURITY_SANDBOX_PATH_DENIED');});
test('sandbox blocks undeclared network capability',()=>{const p=createSandboxPolicy({capabilities:[]});assert.throws(()=>authorizeSandboxAction(p,{capability:'NETWORK',host:'api.example.com'}),e=>e.code==='SECURITY_SANDBOX_CAPABILITY_DENIED');});
