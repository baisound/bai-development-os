import test from 'node:test'; import assert from 'node:assert/strict'; import { generateKeyPairSync } from 'node:crypto'; import { createEd25519SigningProvider, signEnvelopeWithProvider, verifyEnvelopeWithProvider } from '../../src/security/signing.mjs';
test('signing provider abstraction signs and verifies without exposing private key in envelope',async()=>{const {privateKey,publicKey}=generateKeyPairSync('ed25519');const p=createEd25519SigningProvider({private_key:privateKey,public_key:publicKey,key_id:'K1'});const e=await signEnvelopeWithProvider({x:1},p);assert.equal(e.key_id,'K1');assert.equal(await verifyEnvelopeWithProvider(e,p),true);assert.equal('private_key' in e,false);});
