import test from 'node:test';import assert from 'node:assert/strict';
import { createPortableComponentBundle,verifyPortableComponentBundle } from '../../src/release/portable.mjs';import { keys,fixedClock } from './helpers.mjs';
test('portable Knowledge bundle can be signed and verified',async()=>{const k=keys();const b=await createPortableComponentBundle({component_type:'KNOWLEDGE_PACK',component_version:'1.0.0',schema_version:'1.0.0',payload:{items:[1]},clock:fixedClock,signer:k.provider});assert.equal(verifyPortableComponentBundle(b,{public_key:k.public_key,expected_key_id:k.key_id,require_signature:true}),true);});
test('portable payload tamper detected',async()=>{const b=structuredClone(await createPortableComponentBundle({component_type:'AUTOMATION_BOOTSTRAP',component_version:'1',schema_version:'1',payload:{x:1}}));b.payload.x=2;assert.throws(()=>verifyPortableComponentBundle(b),e=>e.code==='RELEASE_PORTABLE_PAYLOAD_MISMATCH');});
test('invalid portable component type rejected',async()=>await assert.rejects(()=>createPortableComponentBundle({component_type:'UNKNOWN',component_version:'1',schema_version:'1',payload:{}}),e=>e.code==='RELEASE_PORTABLE_TYPE_INVALID'));
test('monitoring and connector lock types supported',async()=>{for(const type of ['MONITORING_BUNDLE','CONNECTOR_LOCK'])assert.equal(verifyPortableComponentBundle(await createPortableComponentBundle({component_type:type,component_version:'1',schema_version:'1',payload:{ok:true}})),true);});
