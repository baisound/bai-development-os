import test from 'node:test';import assert from 'node:assert/strict';import { createRepositoryReleasePlan,verifyRepositoryState } from '../../src/release/repository.mjs';
test('repository plan derives standard git tag and owner-gated publish',()=>{const p=createRepositoryReleasePlan({os_version:'1.2.3',repository_url:'https://github.com/baisound/bai-development-os',artifact_names:['bundle.json']});assert.equal(p.git_tag,'v1.2.3');assert.equal(p.external_publish_requires_owner_authorization,true);});
test('invalid repository url rejected',()=>assert.throws(()=>createRepositoryReleasePlan({os_version:'1.0.0',repository_url:'file:///tmp/repo'}),e=>e.code==='RELEASE_REPOSITORY_URL_INVALID'));
test('repository state verification detects remote drift',()=>{const p=createRepositoryReleasePlan({os_version:'1.0.0',repository_url:'https://github.com/baisound/bai-development-os'});const r=verifyRepositoryState(p,{repository_url:'https://github.com/baisound/other',default_branch:'main'});assert.equal(r.verified,false);assert.ok(r.issues.includes('REMOTE_URL_MISMATCH'));});
test('repository state verification passes exact state',()=>{const p=createRepositoryReleasePlan({os_version:'1.0.0',repository_url:'https://github.com/baisound/bai-development-os'});assert.equal(verifyRepositoryState(p,{repository_url:p.repository_url,default_branch:'main'}).verified,true);});
