import test from 'node:test'; import assert from 'node:assert/strict';
import { prepareAutomationRun } from '../../src/automation/index.mjs';
import { modelProfiles, modelRequest } from './helpers.mjs';
const root=process.cwd();
test('prepareAutomationRun composes registry runtime risk startup instruction and plan',async()=>{const r=await prepareAutomationRun({project_id:'BAI',project_root:root,task_id:'TASK-006',role:'Builder',phase:'IMPLEMENTATION',runtime_observation:{platform:process.platform,shell:process.platform==='win32'?'powershell':'bash',probe_evidence:`node:${process.version}`,capabilities:['NODE_RUNTIME']},toolchains:[{name:'node',available:true,capabilities:['JS']}],required_tools:['node'],change_profile:{system_scale:'FOUNDATION',criticality:'FOUNDATION',failure_impact:'CRITICAL'},knowledge_required:false,lifecycle_snapshot:{task_id:'TASK-006',status_revision:1,status:'ACTIVE'},model_profiles:modelProfiles,model_request:modelRequest,task_definition:'implement',role_specification:'builder',actions:[{action_id:'read',kind:'READ'}]});assert.equal(r.result,'AUTOMATION_RUN_PREPARED');assert.equal(r.owner_proposal,null);assert.equal(r.plan.lifecycle_mutation_authority,false);});
test('prepareAutomationRun emits Owner proposal for gated action rather than self-authorizing',async()=>{const r=await prepareAutomationRun({project_id:'BAI',project_root:root,task_id:'TASK-006',role:'Builder',phase:'IMPLEMENTATION',runtime_observation:{platform:process.platform,shell:process.platform==='win32'?'powershell':'bash',probe_evidence:`node:${process.version}`},toolchains:[{name:'node',available:true}],required_tools:['node'],change_profile:{},knowledge_required:false,lifecycle_snapshot:{task_id:'TASK-006',status_revision:1,status:'ACTIVE'},model_profiles:modelProfiles,model_request:modelRequest,task_definition:'implement',role_specification:'builder',actions:[{action_id:'write',kind:'IMPLEMENT_WRITE'}]});assert.equal(r.owner_proposal.owner_approval_required,true);assert.equal(r.owner_proposal.authorization_granted,false);});
