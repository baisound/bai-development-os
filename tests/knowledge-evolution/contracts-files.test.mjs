import assert from 'node:assert/strict';import test from 'node:test';import {readFile} from 'node:fs/promises';
test('knowledge evolution schemas are valid JSON and P3 is not accepted',async()=>{for(const name of ['knowledge-evidence','knowledge-candidate','snapshot-manifest','consumer-evidence-envelope','consumer-evidence-batch','client-policy','delivery-receipt']){const x=JSON.parse(await readFile(new URL(`../../schemas/knowledge-evolution/${name}.schema.json`,import.meta.url),'utf8'));assert.equal(x.$schema,'https://json-schema.org/draft/2020-12/schema');}const e=JSON.parse(await readFile(new URL('../../schemas/knowledge-evolution/consumer-evidence-envelope.schema.json',import.meta.url),'utf8'));assert.deepEqual(e.properties.privacy_level.enum,['P0','P1','P2']);});
test('OpenAPI contract exposes only required MVP endpoints and bearer credential',async()=>{const text=await readFile(new URL('../../specifications/BAI_Knowledge_Hub_Public_Ingestion_API_OpenAPI_Ver1.0.yaml',import.meta.url),'utf8');assert.match(text,/\/v1\/evidence\/batch:/);assert.match(text,/\/v1\/client-policy:/);assert.match(text,/scheme: bearer/);assert.doesNotMatch(text,/api[_-]?key:\s*[A-Za-z0-9]/i);});
