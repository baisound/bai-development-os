import {writeFile} from 'node:fs/promises';
import {createKnowledgeAsset} from '../src/knowledge/index.mjs';
const rows=[
['MISSING_FILE_MISCLASSIFICATION','Missing file misclassification','Treating a missing or unresolved file as proof that the resource does not exist','Resolve through the correct source/registry and report unresolved state rather than absence','Do not infer non-existence from one failed path lookup'],
['PRE_PROBE_ENVIRONMENT_INFERENCE','Pre-probe environment inference','Inferring runtime, shell, filesystem or toolchain before probing the actual execution environment','Probe runtime/environment capabilities before emitting operational commands','Do not infer runtime from UI labels, historical sessions or apparent path syntax'],
['FOUNDATION_ROLE_PATH_OMISSION','Foundation role-path omission','A foundation role or canonical path is omitted from the loaded context and silently bypassed','Resolve all mandatory role/canonical inputs before activation','Do not continue when a mandatory role or authority path is unresolved'],
['EVIDENCE_PATH_MISRESOLUTION','Evidence path misresolution','Evidence is evaluated using an incorrect or stale path','Resolve canonical evidence location and verify checksum/identity before evaluation','Do not classify evidence based on a guessed path'],
['UNTRACKED_EVIDENCE_MISCLASSIFICATION','Untracked Evidence misclassification','Untracked files are assumed to be invalid or irrelevant evidence without checking provenance','Classify evidence by provenance and authority, not Git tracked state alone','Do not equate untracked with untrusted'],
['SHELL_DIALECT_MISMATCH','Shell dialect mismatch','Commands are generated for a shell different from the active runtime transport','Resolve shell provider and command dialect before execution','Do not emit bash/PowerShell syntax based on workspace type alone'],
['AGENT_WINDOW_INLINE_CHAT_RUNTIME_CONFUSION','Agent Window / Inline Chat runtime confusion','Different Chat surfaces are assumed to share the same runtime and path accessibility','Probe the runtime interface independently for each activation surface','Do not transfer runtime assumptions between Agent Window and Inline Chat'],
['HISTORICAL_OUTPUT_COLLISION','Historical output collision','New output overwrites or collides with immutable historical evidence or canonical baselines','Use versioned output paths and collision checks before writing','Do not silently replace historical evidence'],
['TESTER_SOURCE_MODIFICATION','Tester source modification','Independent Tester changes implementation source while verifying it','Tester must report failures and evidence without modifying protected implementation source unless explicitly authorized','Do not mix independent verification with source repair'],
['DURABILITY_ERROR_SUPPRESSION','Durability error suppression','Filesystem durability or persistence errors are swallowed and success is reported','Propagate durability failures and fail closed when required persistence cannot be proven','Do not convert fsync/rename/persistence errors into success'],
['PREMATURE_EVENT_BASED_COMMIT','Premature Event-based commit','A lifecycle event is treated as sufficient evidence to commit or close before all acceptance evidence is satisfied','Evaluate completion and commit against canonical gate and evidence requirements','Do not infer completion solely from one event emission'],
['STRING_TO_BOOLEAN_COERCION','String-to-Boolean coercion','Configuration strings such as false or 0 are coerced as truthy booleans','Validate typed configuration values explicitly at schema boundaries','Do not rely on generic truthiness for policy booleans'],
['UNANNOUNCED_FILENAME_CHANGE','Unannounced filename change','Canonical or expected filenames are renamed without migration and registry synchronization','Use explicit version and migration records and update all canonical references','Do not silently rename canonical documents'],
['HUMAN_MACHINE_AUTHORITY_MISRESOLUTION','Human/Machine authority misresolution','Human-readable companion and machine canonical authority are confused','Resolve authority from document metadata and registry and preserve the designated machine canonical source','Do not infer authority from file format or readability']
];
const clock=()=>new Date('2026-08-08T00:00:00.000Z');
const assets=rows.map(([key,title,mode,safe,prohibited],i)=>createKnowledgeAsset({asset_id:`KA-FAIL-TASK004-${String(i+1).padStart(2,'0')}`,title,knowledge_type:'FAILURE_CASE',status:'CANDIDATE',maturity:'EXPERIENCE',enforcement:'REFERENCE',scope:{level:'DOMAIN',domain:'bai-development-os'},source_trust:'VERIFIED_INTERNAL',sensitivity:'INTERNAL',confidence:.7,tags:['task-004','failure-candidate','foundation'],provenance:{source_ids:['TASK-004'],evidence_refs:['tasks/TASK-004/foundation-improvement-integration-plan.md'],created_by:'TASK-005 migration'},content:{summary:mode,guidance:[safe],required_actions:[],prohibited_actions:[prohibited],verification_steps:['Confirm the failure condition is absent or safely handled before promotion.']},freshness:{review_after:'2026-11-08T00:00:00.000Z',expires_at:'2027-08-08T00:00:00.000Z'},failure:{fingerprint:key,failure_mode:mode,environment_scope:['BAI Development OS foundation'],safe_action:safe,prohibited_inference:[prohibited],evidence_refs:['tasks/TASK-004/foundation-improvement-integration-plan.md'],recurrence_key:key}}, {clock}));
await writeFile('knowledge/seeds/task004-failure-candidates.json',JSON.stringify(assets,null,2)+'\n');
console.log(`generated ${assets.length} failure candidates`);
